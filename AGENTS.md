
Rules:
- The target functionality is outlined in the file `docs/FUTURE_FEATURE_LIST.md`
- Use `virtualenv` when installing new libraries
- Use the `virtualenv` installed in `.venv` when invoking the Python interpreter
- Reinstall the `virtualenv` in `.venv` if it is missing or experiencing issues
- Use the command `.venv/bin/pytest` to run tests
